name: pkg.pr.new

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  publish:
    name: Publish SuperLiora preview
    runs-on: ubuntu-latest
    if: github.repository_owner == 'MoonshotAI' && github.event.pull_request.draft == false

    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build package dependencies
        run: pnpm run build:packages

      - name: Generate SuperLiora built-in catalog
        shell: bash
        run: |
          CATALOG_FILE="$RUNNER_TEMP/liora-built-in-catalog.json"
          node apps/liora/scripts/update-catalog.mjs --out "$CATALOG_FILE"
          echo "SUPERLIORA_BUILT_IN_CATALOG_FILE=$CATALOG_FILE" >> "$GITHUB_ENV"

      - name: Build SuperLiora package
        run: pnpm --filter @superliora/liora run build

      - name: Publish pkg.pr.new preview
        run: pnpm exec pkg-pr-new publish --bin --commentWithSha --packageManager=pnpm,npm --no-template './apps/liora'
