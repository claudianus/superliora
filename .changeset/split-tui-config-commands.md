---
"@superliora/liora": patch
---

Split TUI config slash-command handlers (thinking, appearance, and shared tui.toml persistence) into sibling modules to bring config.ts under the LOC budget. Pure extraction, no behavior change.
