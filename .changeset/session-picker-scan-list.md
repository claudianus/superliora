---
"@superliora/liora": patch
---

Make the session picker a scan list: collapsed rows, an expanded selection, and a window that follows the terminal height. Open it from Command Hub → Sessions or /resume.
