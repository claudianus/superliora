---
"@superliora/liora": minor
---

Add session worktrees, agent dashboard, file rewind, path sandbox, conversation loops, and an extensions hub.

Use `liora --worktree [name]`, `/dashboard`, `/rewind`, `/loop`, and `/extensions`.
