---
"@superliora/liora": patch
---

github-checkout: when HEAD already matches upstream but a prior background install failed for that HEAD, surface rebuild via upgrade/preflight instead of "already up to date"
