---
"@superliora/liora": minor
---

Verify static site deliverables (plain HTML/CSS/JS without package.json) by checking that changed files exist and that changed scripts parse, so such jobs can pass completion verification and project checks.
