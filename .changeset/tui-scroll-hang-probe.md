---
"@superliora/liora": patch
---

Stop mid-scroll freezes from deferred highlight jobs that ignored the paint-clock hold, and add a scroll hang probe (HUD line + SUPERLIORA_TUI_SCROLL_TRACE dumps) so wheel hangs leave evidence.
