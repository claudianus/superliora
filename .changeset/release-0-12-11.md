---
"@superliora/liora": patch
---

Release SuperLiora 0.12.11 with live provider quota in the footer and /quota, a 256-row cap that prunes expired local web-search cache, and Windows `liora upgrade --main` honoring `%LOCALAPPDATA%\SuperLiora\bin` and SUPERLIORA_HOME. Run `liora upgrade` after the GitHub Release publishes.
