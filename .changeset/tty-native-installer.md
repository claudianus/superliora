---
"@superliora/liora": minor
---

Blank-machine install prefers GitHub Release SEA binaries, bootstraps Node locally when missing, and falls back to a source build with browser/computer/retrieval sidecars. Run `curl -fsSL …/install.sh | bash` or `irm …/install.ps1 | iex`.
