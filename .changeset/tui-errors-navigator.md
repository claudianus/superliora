---
"@superliora/liora": minor
---

Add `/errors` (alias `/problems`), a diagnostic navigator that lists failed tool calls and error entries from the session transcript with type-to-filter, and jumps the transcript to the failing entry on Enter while staying open for further jumps.
