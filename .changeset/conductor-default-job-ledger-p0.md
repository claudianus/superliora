---
'@superliora/agent-core': minor
'@superliora/liora': patch
'@superliora/protocol': minor
---

Conductor meta-orchestrator default: Job ledger/pool, live warm pre-spawn, Mission-as-Job bind + async needs_user cards, JobInbox/JobResume, MergeJob trust rules + worktree land-to-main, worker git-push ban, Fleet→Job bridge, `/jobs` + footer strip + job.inbox toasts, capped job-desk injection, and `job.updated`/`job.inbox` protocol events (schemaVersion 1). Core remains the worker waist via SUPERLIORA_PROFILE=core.
