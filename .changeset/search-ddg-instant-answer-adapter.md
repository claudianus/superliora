---
'@superliora/liora': patch
---

Add DuckDuckGo Instant Answer JSON as the first free research search step (no API key). Free fallback tries `api.duckduckgo.com` before the DDG HTML scraper when results are thin.
