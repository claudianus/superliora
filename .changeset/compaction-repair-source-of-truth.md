---
"@superliora/liora": patch
---

Fix compacted history quality checks so a repaired summary is treated as the source of truth, instead of carrying over critical errors from the summary it replaced.
