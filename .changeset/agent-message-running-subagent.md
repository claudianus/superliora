---
"@superliora/liora": patch
---

Let the Agent tool deliver a prompt to a still-running subagent when resuming it by id, as a mid-run message that takes effect at the subagent's next step, instead of failing; the reply arrives with the subagent's result.
