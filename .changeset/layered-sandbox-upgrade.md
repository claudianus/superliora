---
"@superliora/liora": minor
---

Close path-sandbox holes for file tools, Bash path tokens, and Script, and add optional process enforcement (Docker when present). Toggle in Settings → Security, or use `--sandbox-enforcement`, `--no-process-sandbox`, and `SUPERLIORA_SANDBOX_ENFORCEMENT`.
