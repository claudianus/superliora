---
"@superliora/liora": patch
---

DeepResearch accepts `allow_browser` / `budget_usd`, reports real `channels_used`, and returns an offline stub when every channel is empty. Pass `allow_browser: true` or `depth: exhaustive` to escalate to browser search.
