---
'@superliora/liora': minor
'@harness-kit/tui-renderer': patch
---

Upgrade the four TUI chrome surfaces and wire the dormant VFX paths. The tab bar now shows a brand-wave active title with a head-to-tail gradient underline that slides between tabs; the spinner uses a moon-phase glyph set with a themed color pulse; the welcome banner uses a figlet font with a per-letter cascade; and the splash mark gains deterministic texture skins. Idle scenes add a meteor burst on success and a fish-tank feed interaction, and footer tips type in. Every effect keeps a static fallback when effects are off.
