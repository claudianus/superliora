---
"@superliora/liora": minor
---

Auto-build the lean context index when LioraContext or LioraSearch runs, so first exploration no longer falls back to a full workspace scan when the index is missing.

Resume interrupted Ultrawork runs and paused goals from natural follow-up messages, sync resume stage from WorkGraph progress, and retry or switch models after temporary provider API errors during goal turns.
