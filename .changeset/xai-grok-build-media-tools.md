---
"@superliora/liora": patch
---

Wire xAI Grok Build subscription tools for web search, image generation, and video generation. After `/login` with xAI Grok (or `XAI_API_KEY`), `WebSearch`, `GenerateImage`, and `GenerateVideo` use the Build proxy (`cli-chat-proxy.grok.com`) with the official CLI surface headers automatically.
