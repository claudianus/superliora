---
"@superliora/liora": patch
---

Add settings keyword aliases (freeze, FTS, redaction, DDG, …) and jump entries in Command Hub and the command palette. Type those terms in `/settings`, the Hub (?), or Help → Command palette to open the matching pane.
