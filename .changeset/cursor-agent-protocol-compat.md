---
"@superliora/liora": patch
---

Fix Cursor account login chat against the current AgentService host, device checksum, and Connect error trailers so subscription turns stop failing with generic API errors.
