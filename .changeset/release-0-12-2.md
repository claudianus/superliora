---
"@superliora/liora": patch
---

Release SuperLiora 0.12.2 with the Windows PowerShell 5.1 Oh My Posh fix. Run `liora upgrade` after the GitHub Release publishes, then `/host-setup` to refresh the profile.
