---
'@superliora/liora': patch
---

Show prompt-cache miss-reason percentages in `/status` when the session reports them, and add a compact repo-index pill (`idx·warm`, `idx·cold`, `idx·stub-off`) to the footer status bar.
