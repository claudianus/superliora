---
"@superliora/liora": patch
---

Release SuperLiora 0.9.5 with TUI resize/stream fixes, Windows install/runtime Git lookup, resume/worktree recovery, Conductor affinity scheduling, and smart-routing generation preference. Run `liora upgrade` after the GitHub Release publishes.
