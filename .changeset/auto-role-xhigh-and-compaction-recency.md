---
"@superliora/liora": patch
---

Fix AUTO role routing so quality roles prefer XHIGH when that effort is available on the same family, and cheap/compaction roles pick a recent thinking-off model instead of obsolete grok-build SKUs.
