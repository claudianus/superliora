---
"@superliora/liora": patch
---

Release SuperLiora 0.12.18 so coding jobs auto-land without extra verify workers, follow-ups reuse the live job, and `/job mode hotfix` skips the worktree when you are the only coding job. Run `liora upgrade` after the GitHub Release publishes.
