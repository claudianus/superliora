---
"@superliora/liora": patch
---

Release SuperLiora 0.12.12 so Upgrade Studio skips optional sidecar and winget installs that cannot finish without a console or UAC. Run `liora upgrade` after the GitHub Release publishes.
