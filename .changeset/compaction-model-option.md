---
"@superliora/liora": minor
---

Add a `loopControl.compactionModel` option to route context compaction through a cheaper or faster model alias while keeping the main model for the conversation. When unset, compaction keeps using the main model as before.
