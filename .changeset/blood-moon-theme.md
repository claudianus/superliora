---
"@superliora/liora": minor
---

Add the "SuperLiora Blood Moon" dark theme — a cinematic near-black background with crimson red and white accents — and set it as the default theme. To restore the previous auto-detect behavior, set `theme = "auto"` in `~/.superliora/tui.toml`.
