---
"@superliora/liora": minor
---

Speed up the zero-config fallback web search with parallel engines and merged results, and add a shared rate-limit harness that cools down and retries automatically when search engines throttle or block automation. No setup needed — it works with no API keys at all.
