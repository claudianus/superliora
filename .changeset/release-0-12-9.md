---
"@superliora/liora": patch
---

Release SuperLiora 0.12.9 with live TUI frames (welcome, login, prompt editor, thought-orb), chrome-band gradient sweeps, skill-dump/spill caps, Windows shutdown/worktree path fixes, and a one-liner install fix for the omitted checkout-health.mjs bootstrap file. Run `liora upgrade` after the GitHub Release publishes.
