---
"@superliora/liora": patch
---

Parse verify worker dual-axis JSON when axes are flat strings or nested under `verifyVerdict`, including after Korean markdown receipts, so Conductor no longer fails with structured verifyVerdict missing.
