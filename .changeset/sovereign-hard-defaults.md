---
"@superliora/liora": minor
---

Hide legacy tool aliases by default, auto-start the Ch5 research-bridge loopback when enabled, and add real SQLite FTS content search for RepoQuery when SUPERLIORA_REPO_INDEX_ENGINE=sqlite. Show legacy names with SUPERLIORA_SHOW_LEGACY_TOOL_NAMES=1.
