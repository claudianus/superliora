---
"@superliora/liora": minor
---

Plugins now use the Claude Code package format (`.claude-plugin/plugin.json`, skills/commands/agents/hooks/MCP/`bin`). Legacy `kimi.plugin.json` installs are not loaded — convert and reinstall. Run `/plugins` then `/reload`.
