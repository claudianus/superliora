---
"@superliora/liora": patch
---

Release SuperLiora 0.12.10 with live chrome motion on classic ConPTY (header, footer, thought-orb, editor frame). Large-area starfields stay static; Windows Terminal with WT_SESSION stays synchronized. Run `liora upgrade` after the GitHub Release publishes.
