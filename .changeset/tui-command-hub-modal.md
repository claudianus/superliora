---
"@superliora/liora": minor
---

Add a centered Command Hub (`?` / Ctrl-K / `/help`) for modes, model, and sessions. Fix Hub openers on the native input path, slim the main shortcut map, toast when a key is gated, and retry via Hub or `/retry` (no Ctrl-Y conflict with editor redo).
