---
"@superliora/liora": minor
---

Make Conductor the clear control plane: Worker Dock instead of Mission/Fleet, `/jobs dock` and `/jobs bg` instead of `/agents` and `/tasks`, Memory on the Conductor waist, and RepoQuery-only search on that lane. Use `/jobs dock` and `/jobs bg`. Appearance now persists as `worker_dock` (`mission_control` still reads).
