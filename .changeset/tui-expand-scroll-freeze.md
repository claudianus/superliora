---
"@superliora/liora": patch
---

Fix TUI freezes when scrolling with expanded tool output, and make Ctrl+O cycle transcript density (minimal → compact → standard → full). Expanded tool bodies stay windowed so multi-kiloline results no longer stall the terminal.
