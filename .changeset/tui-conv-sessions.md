---
'@superliora/liora': minor
---

Session picker: rename sessions in place with Ctrl+R (inline editor, saves through the existing `renameSession` API) and search across the last prompt and working directory, not just the title.
