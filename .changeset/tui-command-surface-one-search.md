---
"@superliora/liora": minor
---

Make the Command Hub the One-search surface: fuzzy-filter settings jumps, slash commands, and skills in one place. Open with Ctrl-K / Ctrl-Space / ?, then type. The nested command palette is removed — same catalog, one search box.
