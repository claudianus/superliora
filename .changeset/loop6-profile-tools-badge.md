---
'@superliora/liora': patch
---

`/profile` and Tools Session (live) status now show a diagnostic badge `profile=<name> tools=<n>` (core=12, agent=30) so guide/sensor mismatches (e.g. Goal tools missing on Core) are visible without reading wire logs.
