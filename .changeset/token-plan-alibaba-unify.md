---
"@superliora/liora": minor
---

Treat Qwen Cloud Token Plan and the models.dev `alibaba-token-plan` entries as one service: model lists now resolve live from the models.dev catalog, a China-region endpoint lane is offered in the provider picker, and `ALIBABA_TOKEN_PLAN_API_KEY` works wherever `QWEN_TOKEN_PLAN_API_KEY` does (chat, image, and video generation). Run `/provider` and pick the Token Plan row to connect.
