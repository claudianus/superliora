---
"@superliora/liora": minor
---

Add Upgrade Studio for `/upgrade`: live stage checklist and progress during install, center-modal plan/success/fail UX, CLI TTY install theatre, and stage markers in `install.sh`.
