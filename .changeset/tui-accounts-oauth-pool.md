---
"@superliora/liora": minor
---

Add the `/accounts` slash command (also under Settings → Accounts) to list, promote, label, and remove OAuth account-pool slots in the TUI. Run `/accounts` or open Settings → Accounts.
