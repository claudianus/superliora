---
"@superliora/liora": minor
---

Remove the /provider slash command. Provider setup is now handled by /login in the TUI and `liora provider` in the shell, which already covered the same add/remove flows.
