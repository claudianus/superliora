---
'@superliora/agent-core': patch
---

test(agent-core): pin agent/injection/tool-workflow pure helper regression cases

- `hasToolWorkflowSurface` any-on / all-off branches.
- `buildToolWorkflowGuidance` enabled sections + empty-capability reminder.
- `buildToolWorkflowSparseGuidance` length + empty-capability fallback.
