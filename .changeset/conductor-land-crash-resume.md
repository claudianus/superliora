---
"@superliora/liora": patch
---

Fix Conductor job landing so merge uses the real kaos git runner, and mark stuck running jobs as interrupted on session resume so `/job resume` works after a hard exit.
