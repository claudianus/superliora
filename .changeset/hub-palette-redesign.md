---
"@superliora/liora": minor
---

Redesign the Command Hub as a single-column palette: always-visible search row with match count, rows with inline descriptions and right-aligned state badges, fuzzy match highlighting, and paged scrolling that fits the terminal height. Space now types multi-word search once a query exists, and digits type into search instead of triggering hidden hotkeys. Open the Hub with Ctrl-K to see it.
