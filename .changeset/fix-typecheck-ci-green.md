---
"@superliora/liora": patch
---

Fix monorepo typecheck failures that blocked CI, including missing CLI imports, provider env cases, TUI fixture typing, service type re-exports, and source-level types for the native TUI renderer package.
