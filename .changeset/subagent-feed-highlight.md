---
'@superliora/liora': minor
---

Add syntax-highlighted mini previews to the background subagent activity feed: Write/Edit calls now render the first lines of their content with the same highlighter and path-based language detection as the main agent's tool cards, within the existing per-agent line budget.
