---
"@superliora/liora": patch
---

Fetch the live Cursor model catalog after account login so the model picker lists current Composer, Claude, GPT, Gemini, and Grok variants instead of a short static preset. Re-login with Cursor, or wait for the background provider refresh, then open the model picker.
