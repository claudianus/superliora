---
"@superliora/liora": patch
---

Move Fleet orchestration modules under the fleet package path and add a Ch5 research-bridge loopback /search server for non-empty local hits. Run the host with --serve when using SUPERLIORA_CHROME_RESEARCH_BRIDGE=1.
