---
"@superliora/liora": patch
---

Show OpenRouter, OpenCode Zen, and Z.AI in `/login`, send thinking `max` as `max`, store detected keys as `{env:VAR}`, and polish Copilot/Anthropic/Cursor/xAI login fallbacks. Run `liora upgrade` after the GitHub Release publishes.
