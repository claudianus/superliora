---
"@superliora/liora": patch
---

Add multi-account OAuth login for non-Kimi providers in `/login` (for example xAI Grok and OpenAI Codex). When already signed in, choose "Add another account" so quota/rate-limit failures can auto-switch across the account pool.
