---
"@superliora/liora": minor
---

Smart auto routing now picks models with a shared scorer, failover chain, and optional session `auto` pin. Open Settings → Model routing, or pick Smart Auto in the model picker (`/model auto`).
