---
"@superliora/liora": patch
---

Route xAI account logins through the Grok Build chat proxy by default so usage counts as Grok Build, while still allowing the public Grok API path via `providers.xai-grok.base_url = "https://api.x.ai/v1"`.
