---
"@superliora/liora": patch
---

Send thinking effort `max` as `max` on OpenAI-compatible requests instead of rewriting it to `xhigh`, so GPT-5.6, DeepSeek, and OpenCode Zen keep the real top rung.
