---
"@superliora/liora": patch
---

Release SuperLiora 0.12.17 with a Windows host-browser EINVAL fix so visual proof is skipped_host instead of a product fail, and land playable dests when tests pass. Run `liora upgrade` after the GitHub Release publishes.
