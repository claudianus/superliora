---
"@superliora/liora": patch
---

Fetch the xAI Grok model list from the models.dev catalog after signing in with an xAI account, so newly released models (Grok 4.5) appear without a new release. Falls back to the built-in preset, which now also includes Grok 4.5, when the catalog is unavailable.
