---
"@superliora/liora": patch
---

Mark rejected OAuth credentials unhealthy, filter them from role/route candidates, expand same-capability fallbacks across logged-in providers, and emit `provider_route_switch` when a turn fails over.
