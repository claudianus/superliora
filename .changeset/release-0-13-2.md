---
"@superliora/liora": patch
---

Fix Windows workers missing bundled node/pnpm when SuperLiora home is redirected, and stop retrying stalled LLM streams. Run `liora upgrade` after the GitHub Release publishes.
