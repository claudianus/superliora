---
"@superliora/liora": patch
---

Fix GitHub source auto-update: keep the checkout on the tracking branch (no detached FETCH_HEAD), and sync the lockfile with agent-core optional embedding deps so frozen install can finish.
