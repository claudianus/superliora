---
"@superliora/liora": patch
---

Split more TUI coordinator logic into focused controllers (autocomplete, startup lifecycle, shell input, session requests, app state, native renderer diagnostics).
