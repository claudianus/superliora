---
"@superliora/agent-core": patch
---

**Media tool descriptions**

- Document GenerateImage default size **1024x1024** / provider auto and GenerateVideo **16:9 · 5s** in tool docs so the model sees zero-config defaults without extra config.
