---
"@superliora/liora": patch
---

Refresh Alibaba Token Plan defaults to `qwen3.8-max`, add `deepseek-v4-flash-0731`, keep image models on wan2.7, and derive media API hosts from the configured Token Plan base URL. `/status` detects Token Plan env keys for Media readiness.
