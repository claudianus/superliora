---
"@superliora/liora": minor
---

Windows install now sets up Windows Terminal for the TUI: detect or install `wt.exe`, write a SuperLiora fragment (Cascadia Mono), and add a Start Menu shortcut. Failures never block the CLI. Skip with `--no-terminal` or `SUPERLIORA_NO_TERMINAL=1`.
