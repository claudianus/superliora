---
"@superliora/liora": patch
---

Fix Cursor subscription chat against the current AgentService host, device checksum, and Connect error trailers so turns stop failing with generic API errors. Run `liora upgrade` after the GitHub Release publishes.
