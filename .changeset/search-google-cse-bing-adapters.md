---
'@superliora/liora': minor
---

Add Google Custom Search and Bing Web Search v7 to the research search provider matrix. Set `GOOGLE_CSE_API_KEY` (or `GOOGLE_API_KEY`) plus `GOOGLE_CSE_ID` (or `GOOGLE_CSE_CX`), or `BING_SEARCH_API_KEY`, to enable them before the free fallback.
