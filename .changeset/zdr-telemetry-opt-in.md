---
'@superliora/telemetry': minor
'@superliora/liora': minor
---

Make product telemetry opt-in by default (ZDR-friendly). Analytics only enable when `telemetry = true` is set in config.toml; missing or broken config stays off.
