---
"@superliora/liora": patch
---

Infer custom endpoint wire type from URL path suffixes and let `/login` pick openai, openai_responses, anthropic, and other types. Use path suffixes like `/v1/responses` or choose Type with ←→ in the dialog.
