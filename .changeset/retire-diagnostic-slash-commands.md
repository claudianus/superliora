---
"@superliora/liora": major
---

Remove beginner-hostile diagnostic slash commands `/bench`, `/renderer`, `/term`, `/export-debug-zip`, and `/improve-harness`. Keep `/aquarium` and `/feed`. Use `/export-md` for transcript export and `SUPERLIORA_NATIVE_RENDERER_DIAGNOSTICS=1` for the renderer HUD.
