---
"@superliora/liora": patch
---

Align `liora upgrade` for GitHub source installs with `install.sh`: shallow fetch, full package build, and wrapper refresh. Run `liora upgrade` after pulling from source.
