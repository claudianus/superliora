---
"@superliora/liora": patch
---

Show subagent completions and failures in headless mode so an UltraSwarm run no longer produces invisible specialist work. Stop background tasks held by agents that were never lazily resumed during session close, instead of skipping them. Forward tool results into the UltraSwarm progress grid so specialist tool outcomes are visible alongside tool starts.
