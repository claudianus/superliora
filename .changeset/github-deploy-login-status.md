---
'@superliora/liora': minor
---

Add the /github-connect slash command to check the GitHub CLI login that git push, repo create, and Pages deploy rely on, and surface an exact fix in the push job when credentials block it. Run /github-connect to see the login state.
