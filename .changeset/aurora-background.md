---
"@superliora/liora": minor
---

Add an ambient aurora background — a slow, low-intensity color wash behind the whole terminal that moves gently over time. It is on by default and turns off automatically over SSH, in CI, or when the terminal reports no color support.
