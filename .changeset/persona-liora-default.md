---
"@superliora/liora": minor
---

Add the Liora persona preset and make it the default when no persona is configured. Switch with `/persona set <preset>`, or set `preset = "none"` in config.toml to disable.
