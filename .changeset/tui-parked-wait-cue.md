---
"@superliora/liora": patch
---

When the turn is blocked on TaskOutput, the status row stays a calm waiting cue instead of the busy spinner. It reads `waiting · ctrl+s: steer` (or the still-running watcher counts) until the wait ends.
