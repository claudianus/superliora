---
"@superliora/liora": minor
---

Port kimi-code 0.22.x stability fixes and UX improvements: abandoned tool exchange teardown, headless subagent drain, session index rebuild, Gemini thought-signature round-trips, image auto-compression, compaction handoff, model alias overrides, paste-burst fallback (`disable_paste_burst` in tui.toml), bash-mode input history, and subagent card spinner.
