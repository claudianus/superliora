---
"@superliora/agent-core": patch
---

**WebSearch provider defaults**

- Local and Moonshot web search providers fall back to **3** results when limit is omitted, matching the WebSearch tool schema default (was 5).
