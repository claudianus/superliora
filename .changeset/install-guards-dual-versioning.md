---
"@superliora/liora": patch
---

Fix source-install startup when the bundled CLI left a mistyped `@superliora/superliora-sdk` import unresolved, add install/release guards, and track the upstream Kimi Code baseline separately from the SuperLiora release version. Run `/status` to see the current upstream sync line.
