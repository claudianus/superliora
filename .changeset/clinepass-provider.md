---
"@superliora/liora": minor
---

Add ClinePass as a built-in OpenAI-compatible provider for the curated open-weight coding models behind Cline's subscription. Connect it from `/login` or with `liora provider catalog add clinepass --api-key "$CLINE_API_KEY"`.
