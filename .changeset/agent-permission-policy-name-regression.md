---
'@superliora/agent-core': patch
---

test(agent-core): pin agent/permission/policies/{agent-swarm-exclusive-deny, goal-start-review-ask} policy-name regression cases

- `AgentSwarmExclusiveDenyPermissionPolicy.name` — pin the documented name (`agent-swarm-exclusive-deny`).
- `GoalStartReviewAskPermissionPolicy.name` — pin the documented name (`goal-start-review-ask`).
