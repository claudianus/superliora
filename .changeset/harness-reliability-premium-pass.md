---
"@superliora/liora": minor
---

Harness reliability and premium TUI pass: Edit fuzzy matching hardened with Levenshtein-based candidate rescue, local recall store integrity checks with mirror recovery, compaction block observability counters, transient no-body provider errors (HTTP 400 with empty body) now classified as retryable with subagent model fallback across up to two hops reported via retryAttempt/retryLimit/fellBackToModel on subagent.failed events, and TUI upgrades covering agent swarm progress frames, usage panel, goal monitor, footer context, and new status glyphs.
