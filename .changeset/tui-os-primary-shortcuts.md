---
"@superliora/liora": patch
---

Recognize macOS Cmd as the TUI primary modifier (same chords as Ctrl on Linux/Windows), and keep empty-prompt ↑ on prompt history instead of the worker dock unless a dock row is selected.
