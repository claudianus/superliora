---
"@superliora/liora": patch
---

Install and upgrade default to published releases; opt into tip of `origin/main` with `--main` (`liora upgrade --main`, `/upgrade --main`, or `install.sh --main`).
