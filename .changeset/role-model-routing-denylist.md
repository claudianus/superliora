---
"@superliora/liora": minor
---

Remove the DeepSeek auto-routing denylist, always honor Settings → Model routing overrides, and show each role’s resolved auto pick in Model routing and `/status`. Open Settings → Model routing to set Planning/Coding and see `auto → <alias>`.
