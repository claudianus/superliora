---
"@superliora/liora": minor
---

Worker Dock: goal-lane ledger rows (goal-desk / goal-driver) now show live telemetry and honest status instead of a bare title. Desk rows mirror their driver's phase, provenance chips (`desk` / `driver`) replace the blank model column, and the misleading "suspended — waiting for a pool slot" / "finishing" copy for ledger ghosts was replaced with actionable notes (e.g. `paused — /goal resume to continue`).
