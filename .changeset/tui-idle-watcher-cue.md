---
"@superliora/liora": patch
---

Keep the turn-status row up after a turn ends when background commands or subagents are still running. It shows counts such as `1 command still running` until that work finishes.
