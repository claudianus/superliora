---
"@superliora/liora": minor
---

Stream live truncated child-tool stdout onto the parent event stream and into ACP tool cards while a command is still running. Use `liora acp` to see the live cards in an ACP client.
