---
"@superliora/liora": minor
---

Press `w` in the `/diff` review dialog to soft-wrap long diff lines instead of truncating them. Continuation rows carry a six-column indent, wrapped output keeps its syntax coloring, and the footer shows whether wrap is on.
