---
'@superliora/agent-core': patch
---

test(agent-core): pin agent/plan/ultra-plan-section-guidance + seed-build + ultra-swarm-routing regression cases

- `ULTRA_PLAN_REQUIRED_SECTIONS`, `ULTRA_PLAN_SECTION_GUIDANCE`,
  `MAX_INTERVIEW_ROUNDS` cover canonical 10 sections, per-section labels
  and ask hints, and the soft interview cap.
- `buildSeedSpec` covers default evaluation principles, constraint /
  ontology isolation from caller mutations, autoGenerated flag, custom
  timestamp, and deterministic AC numbering.
- `intensityToDefaultExpertCount` and `routeFromPlanSignals` cover
  ENGAGE / ADAPTIVE / DEFER routing, DEFER → ADAPTIVE escalation via
  `--swarm` and `force swarm: yes`, default expert counts, and explicit
  case-insensitive `swarm intensity` overrides.
