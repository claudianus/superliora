---
"@superliora/liora": patch
---

Keep compiler stacks and stray stdout/stderr out of the TUI prompt. Diagnostic dumps stay in the session log instead of overwriting the draft. Run `liora upgrade` after the GitHub Release publishes.
