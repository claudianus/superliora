---
"@superliora/liora": minor
---

Regular vs Ultra plan now auto-routes from the task context (or explicit `ultra`). Ultra interviews follow Ouroboros-style Socratic quality with a faster interview→write path. Pass `initial_context` on EnterPlanMode; use `ultra: true` for vague/greenfield work and `ultra: false` for scoped fixes.
