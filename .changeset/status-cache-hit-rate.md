---
'@superliora/liora': minor
---

Show the session prompt-cache hit rate in the `/status` report. The status panel now renders a "Cache hit" row (percentage) sourced from the session usage recorder, so you can see how much of the prompt prefix is served from cache. The row is omitted until usage data exists.
