---
"@superliora/liora": patch
---

When `mission.autoStart` is ON, the status bar reminds you to run `/mission resume` or `/mission <objective>` on session open — Mission still never auto-starts without evidence.
