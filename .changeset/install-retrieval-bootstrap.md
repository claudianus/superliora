---
"@superliora/liora": patch
---

Install and auto-update now pull the local Granite-97M embedder and precompute SearchExpert/SearchSkill passage indexes (skip with `--no-retrieval` or `SUPERLIORA_SKIP_RETRIEVAL=1`).
