---
"@superliora/liora": minor
---

Add experimental Cursor account login and Connect-RPC chat provider so you can use Cursor models from SuperLiora. Run `/login`, pick Cursor, then choose a model (disable with `SUPERLIORA_EXPERIMENTAL_CURSOR_OAUTH=false`).
