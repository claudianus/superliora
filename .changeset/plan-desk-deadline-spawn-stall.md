---
"@superliora/liora": patch
---

Plan Desk / mission jobs use a 45m wall-clock (implement stays 30m); post-spawn 120s no-progress stalls mark blocked + inbox; needs_user / resume inherit remaining budget. Spent remaining is 1ms (never timeoutMs 0, which disables the deadline).
