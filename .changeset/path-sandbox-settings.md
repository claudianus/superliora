---
"@superliora/liora": minor
---

Add a Settings → Security path sandbox (off / workspace / read-only) for file tools. This is not OS isolation. Toggle in Settings → Security, or use `--sandbox`, `SUPERLIORA_SANDBOX`, `config.toml sandboxProfile`, or `local.toml workspace.sandbox_profile`.
