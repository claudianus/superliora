# Test baseline ratchet — expected failures. Update only via:
#   node scripts/check-test-baseline.mjs --update
version: 1
packages:
  - dir: apps/liora
    runner: vitest
    failures: []
    unstable:
      # Premium-motion frame sampling: under heavy load the animation clock
      # can stall between the two sampled frames (failed once on a loaded
      # windows CI runner, 2026-08-31; passes locally and on most runs).
      - 'FooterComponent tip crossfade > types rotating tips in left-to-right instead of hard-swapping under premium'
