# Archived reports

One-off design/analysis reports written during earlier development sessions
(added in a single commit on 2026-06-27, not updated since). They are kept
here for historical reference only — they are not maintained documentation
and may be out of date. Do not treat them as the source of truth; see the
actual code under `packages/` and `apps/` instead.
