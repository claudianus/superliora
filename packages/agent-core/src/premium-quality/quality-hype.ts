/**
 * Compact quality pressure for Premium mode.
 * Operational contracts live in visual-harness / guidance; this file only sets tone.
 * Keep short: synonym floods burn tokens without changing behavior.
 */

export const PREMIUM_QUALITY_HYPE_BANNER = [
  '🔥 PREMIUM QUALITY — screenshot-proof, anti-slop, principal-designer bar 🔥',
  'Ship principal-designer-bar results. Mediocre / MVP-looking output fails this mode.',
].join('\n');

export const PREMIUM_QUALITY_HYPE_MANTRA = [
  '### Quality bar',
  'If a principal designer would reject the first-glance screenshot, it is NOT done — iterate until it is illegally good.',
].join('\n');

export const PREMIUM_QUALITY_HYPE_VISUAL_FLOOD = [
  '### Visual directives (PRIMARY)',
  'Visible surfaces need real hierarchy, spacing, motion, assets, states — not placeholders. Primitive geometry as final art fails Premium.',
].join('\n');

/** Compact hype for sparse injection turns. */
export const PREMIUM_QUALITY_HYPE_SPARSE =
  'Premium still ON — keep visuals screenshot-proof and anti-slop until principal-designer bar is met.';
