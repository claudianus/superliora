/**
 * Premium Quality tone pressure — screenshot-proof craft, not award-bait scope inflate.
 * Operational contracts live in visual-harness / guidance; this file is the tone weapon.
 */

export const PREMIUM_QUALITY_HYPE_BANNER = [
  'Premium Quality is ON — hold a screenshot-proof craft bar on user-visible surfaces.',
  'Ship complete, polished work inside the stated scope. Raise craft; do not expand scope for spectacle or award-bait.',
  'Placeholder, generic template, and "MVP-looking" UI fail this mode unless the user asked for a wireframe.',
  'Mediocre is not done. Iterate craft until first glance reads intentional — without inventing extra features.',
].join('\n');

export const PREMIUM_QUALITY_HYPE_MANTRA = [
  '### Quality mantra (repeat internally every pass)',
  'SCREENSHOT-PROOF. ZERO-SLOP. ZERO-PLACEHOLDER. INTENTIONAL HIERARCHY. COHESIVE ART DIRECTION. REAL STATES.',
  'Visuals: clear hierarchy, deliberate spacing/type/color, coherent motion/feedback, anti-generic assets.',
  'If first glance still reads as placeholder/generic, it is not done — iterate craft inside the stated scope.',
].join('\n');

export const PREMIUM_QUALITY_HYPE_VISUAL_FLOOD = [
  '### Visual craft directives (PRIMARY — every user-visible surface)',
  'Build modern, designer-grade, cohesive interfaces: hierarchy, spacing, type, color, motion, states, assets.',
  'Heroes: one dominant visual and clear hierarchy — not template defaults or decorative noise.',
  'Games and canvas: themed, juicy feedback, cohesive art direction — not homework geometry unless requested.',
  'Flat primitive art (e.g. bare pink field + yellow circles) fails unless the user asked for a wireframe — replace with intentional craft.',
].join('\n');

/** Compact reminder for sparse injection turns. */
export const PREMIUM_QUALITY_HYPE_SPARSE =
  'Premium Quality still ON — screenshot-proof craft inside scope; zero slop/placeholder; iterate until first glance is intentional.';
