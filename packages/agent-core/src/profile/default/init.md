You are a software engineering expert. Explore the project directory to understand its architecture and main details.

Task requirements:
1. Identify key config files (`pyproject.toml`, `package.json`, `Cargo.toml`, etc.), tech stack, build/runtime architecture, module layout, and project-specific conventions, testing, and deployment.
2. Summarize findings and overwrite the root `AGENTS.md`, building on what is already there.

`AGENTS.md` is read by AI coding agents who know nothing about the project. Base it only on actual project content—no assumptions. Use the language dominant in project comments and documentation.

Common sections: project overview, build/test commands, code style, testing instructions, security considerations.
