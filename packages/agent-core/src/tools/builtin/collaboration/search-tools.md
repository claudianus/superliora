Search the active tool registry by name or description (eyes for the tool surface).

Use when unsure which dedicated tool fits before improvising with Bash. Returns matching tools with active/source flags. Query is a case-insensitive substring over name + description; empty query lists active tools (capped).
