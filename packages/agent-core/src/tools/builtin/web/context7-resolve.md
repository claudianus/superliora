Resolve a library name to a Context7-compatible library ID before fetching docs.

Use for API docs, setup steps, or code examples. Call before Context7Docs unless the user already gave a library ID. Provide `library_name` and `query` (task/question). Pick the best match, then call Context7Docs.
