Fetch a public `http`/`https` URL. HTML → main article text; plain/markdown → full body. Result states which form. No private/loopback/other schemes. Large pages may truncate or refuse. Incomplete extract (needs render/select) → Scrapling-compatible MCP/CLI/browser for authorized public pages.
