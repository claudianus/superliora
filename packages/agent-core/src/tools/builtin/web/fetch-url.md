Fetch a public `http`/`https` URL. HTML → extracted main article as Defuddle-cleaned markdown (structure kept, chrome stripped); plain/markdown → full body returned verbatim. Result states which form. No private/loopback/other schemes. Large pages may truncate/refuse. Incomplete extract → Scrapling-compatible MCP/CLI/browser for authorized public pages. Prefer primary source URLs.
