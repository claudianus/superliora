Generate an image from a text prompt into the workspace.

Icons, mockups, hero/product shots. Prefer over ad-hoc scripts when a provider is ready.

Provider: default `auto` (first ready: xAI Grok Build/OAuth → Qwen Token Plan → Codex → OpenAI Images → Gemini). Force only a ready id; unavailable force falls back to auto. Default `.superliora/generated/images/<timestamp>.png`. Report path; use ReadMediaFile when image input is available.
