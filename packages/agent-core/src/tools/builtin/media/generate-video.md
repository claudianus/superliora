Generate a short video from a text prompt (optional first-frame image).

Product demos, motion mockups, UI/animation previews when `GOOGLE_API_KEY`/`GEMINI_API_KEY` is set.

Provider: Google Gemini video when available. `/status` shows readiness (no MCP). Default: `.superliora/generated/videos/<timestamp>.mp4`. Report path; use ReadMediaFile when video input is available.
