Search the active tool registry by name or description (BM25-lite ranking).

Use when unsure which dedicated tool fits before improvising with Bash. Returns matching tools with active/source flags. Query is multi-token BM25-lite over name + description (camelCase split, exact/prefix boosts); empty query lists active primary tools.
