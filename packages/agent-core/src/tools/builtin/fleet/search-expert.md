Search the expert catalog for delegation candidates. Call it directly — not as an expert name.

Returns metadata; cite candidate names/IDs as context when delegating the work they match. Use 3–12 English keywords (translate non-English intent). Broaden once or raise top_k to 12 if weak.

Use when the Ultrawork coverage matrix has material lanes, or for multi-domain work. For engineering/TUI/CLI tasks, prefer English technical keywords and optionally set `division` to `engineering` or `design`. Avoid vague words like `coach`/`growth`/`pipeline`.
