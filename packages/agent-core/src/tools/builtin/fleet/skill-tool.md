Invoke one registered skill by exact name. Use `SearchSkill` first for discovery.

Loaded skills are reference material, not mandatory scripts. Apply only quality-improving parts. Do not re-invoke a skill to repeat work already done: if a `<kimi-skill-loaded>` block with the same `args` is present, reuse it.
