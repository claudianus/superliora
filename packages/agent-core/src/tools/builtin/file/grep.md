Search file contents with ripgrep regex. Unknown content/location only — known path → `Read`. ALWAYS use Grep instead of shell `grep`/`rg`.

Ripgrep syntax (not POSIX); escape `\{` for literal braces. hidden/dotfiles on by default. `include_ignored=true` for `.gitignore` paths; sensitive files (`.env`) always skipped.
